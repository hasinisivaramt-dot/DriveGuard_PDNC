# models/

Serialized trained model artifacts (scikit-learn .pkl, PyTorch .pt) plus a
model_versions manifest matching the MongoDB `model_versions` collection
schema (version, algorithm, metrics, trained_at, artifact_path).
